# Sys Script 3
import subprocess
import os

def main():
    subprocess.call('clear')
    subprocess.call(['echo', 'You are currently in the'])
    subprocess.call('pwd')
    subprocess.call(['echo', 'directory'])
main()
